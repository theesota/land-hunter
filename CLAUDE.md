# とちげっちゅ(旧 土地ハンター)

伊勢崎市で家を建てる土地を、歩いて探して家族3人(ソータ・ミホ・お義母さん)で共有するスマホ用ブラウザアプリ。
ソータは非エンジニア。使うのは iPhone Safari。公開URL https://theesota.github.io/land-hunter/

## 運用
- このリポジトリ(main)が正。`/home/user/tasobow` の `docs/` はミラー。同期とテストの手順は `HANDOFF_land-hunter.md` §5
- 変更したら `tests/` を回し、両リポジトリに push し、公開URLに反映されたことを curl で確認する

## UI文言のルール(ソータの指示。守らないと「ダサい」「説明が多い」と返ってくる)
- 説明しすぎない。ラベルは名詞1語。「住所(自動取得を直せます)」ではなく「住所」
- 送りがなを足さない。「同期済み」の「み」は要らない。そもそも常時出る状態表示(同期済など)は出さない
- 注意書き・補足の段落を足さない。必要な補足は1文以内
- 状態は色や有無で伝え、文で説明しない
- 「〜しました」「〜できます」「〜してください」を避け、短い体言止めか2〜4文字の動詞で終える(例: 「保存」「ゴミ箱へ」)
- 見出し・ボタン・項目名は普通の漢字。ひらがなにするのはアプリ名「とちげっちゅ」だけ
- 迷ったら削る。足す前に「これは要るか」を問う

## デザインの決まり
- ヘッダーは濃いグレー(`--bar`)。緑は主ボタン(登録・カメラ)だけ
- 地図上の丸ボタン(検索・コンパス・カメラ・現在地)は全部44pxで統一。左上/右上は対称、右下は端に寄せる
- ズームボタンは出さない
- パネル(表示)は右半分まで。メニュー・一覧・共有・設定は全画面の1ページ。詳細は下からのシート
- フォント: Zen Maru Gothic(タイトルは900)。手書き風の丸文字は読みづらいので使わない
