# HANDOFF_land-hunter(土地ハンター)_session_2026-09-12.md

追記型。§0は毎セッション差し替え、§1以降は日付見出しで追記。

## §0 次セッションの初手(2026-09-12時点)

- 【必ず最初に詰める】ブロッカーは無し。直近の機能は全部deploy済みで、ソータの実機確認待ちのものが2つある
  1. 「建てられる目安」(坪数×建ぺい率/容積率)が実機の登録シートで出るか
  2. カメラモードで「カメラで撮る」→ 現在地にピンが立つか(位置情報の許可はソータ側で解決済み)
- 再開手順:
  1. `git -C /home/user/land-hunter pull origin main`(作業の正はこのリポジトリ。公開URL https://theesota.github.io/land-hunter/)
  2. `/home/user/tasobow`(ブランチ `claude/land-search-sharing-app-rxa9k6`)は `docs/` にミラーするだけ。同期コマンドは§5
  3. 直したらテスト(`tests/README.md`)→ 両リポジトリにpush → `curl` で公開URLに反映されるまで待つ(GitHub Pagesは反映まで1〜2分、ブラウザ側キャッシュ10分)
  4. ソータからの次の要望は「一個ずつ潰す」方式。受けたら実機の症状を先に聞かず、コードとデータで再現して直す
- 前提の再確認: ソータは非エンジニア。iPhone Safariで使う。妻ミホとお義母さんに共有予定だが、まだ自分ひとりで試している段階(2026-09-12時点)

## §1 完了事項(2026-09-12 このセッション)

### 位置情報
- ✅ 位置情報の許可案内シート(端末別の手順、22秒で打ち切り、診断行「許可=/エラー=/https/端末」)
- ✅ Safariは「タップの中」でしか許可ダイアログを出さない → 現在地ボタンもカメラも同期で位置取得を開始するよう修正
- ✅ 実機で効いた手順に案内を書き換え: 設定→プライバシーとセキュリティ→位置情報サービス→Safari Webサイト→このAppの使用中のみ許可
- ✅ ソータの端末は上記で解決済み(診断は denied/エラー1 だった)

### 共有・データ
- ✅ URLからボードIDを消さない(`#b=...`を常に残す)。localStorage消失で別ボードが黙って作られる事故を防止
- ✅ 初回起動で新規ボードを作ったときの案内シート(招待リンクで参加できる)
- ✅ 別ボードで貯めた地点を、参加時(フォーム/招待リンクどちらでも)に自動で持ち込む
- ✅ 同じタブのアドレスバーに招待リンクを貼った場合(hashchange)も読み直す
- ✅ ゴミ箱(ソフト削除 deletedAt)。戻す/完全に削除/ゴミ箱を空にする。最後の1件の完全削除が復活するバグも修正(serverSeen)
- ✅ 共有IDタップで招待リンクをコピー

### 地点の情報・UI
- ✅ 詳細をポップアップから画面下のシート(高さ55%)に変更。ピンが隠れない位置に地図を寄せる
- ✅ 絵文字→SVGアイコン。学区は設定の表示項目に連動(既定は小学校区のみ)。施設は1行ずつ
- ✅ 住所の手直し欄(自動取得は町丁目まで)
- ✅ 検索欄で緯度経度(度分秒/10進)・GoogleマップURL・郵便番号付き住所を受け付け、ピンポイントなら登録確認まで進む。国土地理院の住所検索は番地まで効く(〒付きは空振りするので落とす)
- ✅ カメラモード(右下の緑ボタン)。EXIF GPS→現在地→地図タップの3段。iOSはブラウザ撮影にGPSが付かない(Apple仕様)
- ✅ 坪数・売り値(万円)・上水道・下水道の入力。坪単価自動計算。一覧にも表示
- ✅ 区域区分(市街化区域/調整区域)と用途地域(建ぺい率・容積率)。データは国土数値情報A55 2024年度の伊勢崎市分(市提出)。表示チップ「調整区域」「用途地域」+登録時の自動判定
- ✅ 建てられる目安: 坪数×建ぺい率=1階最大、×容積率=延床最大(㎡併記、前面道路の注記)

### インフラ
- ✅ テスト一式を `tests/` に退避(scratchpadから)。`tests/README.md`に実行手順

## §2 判断記録(2026-09-12)

- [認証は入れない] — 根拠: 使うのは3端末(ソータ・ミホ・お義母さん、各自スマホ)。URL固定で十分。ログイン画面のコストの方が高い
- [削除はゴミ箱方式] — 根拠: ソータ指示。共有中に誰かが誤って消しても他の人が戻せる。完全削除だけ確認ダイアログ
- [ゴミ箱の自動掃除は入れない] — 根拠: お義母さんが消したものを後で見返す可能性。消えたら戻らない側に倒す
- [詳細は下シート、設定/一覧は右半分パネル] — 根拠: ソータの「右半分ルール」はパネル用。詳細はピンが上寄りだと隠れるので下シート
- [区域区分データはA55 2024年度(市提出)] — 根拠: 市の2026年4月都市計画図はJPEGで座標なし、GISデータ非公開。座標付きで最新なのはこれ。2018年A09は廃止
- [中学校区は既定OFF] — 根拠: ソータ「中学校はいらない、表示の選択で区別」
- [住所は手入力で補正、地番の自動表示は保留] — 根拠: 地番は法務省登記所備付地図データで取れるが「地番が出て、で、どうする」が未定。ソータの購入導線(不動産屋/看板/登記)次第
- [カメラの位置は撮影前に取得開始] — 根拠: Safariはユーザー操作の中でしか許可ダイアログを出さない(developer.apple.com forums)

## §3 監督の推し

- 次に価値が高いのは「実効面積」(建ぺい率の上限から駐車場2台分と隣地離れを引いた、現実に置ける1階面積)。細長い土地や旗竿地で坪単価の錯覚を潰せる。ソータに車の台数を聞いてから
- ミホ・お義母さんへの共有時、LINE文に「最初に位置情報を聞かれたら『許可』」を入れる定型文を招待画面に付けると、今回の位置情報の往復が消える
- 歩きながら撮る運用に変わるなら、ステータス初期値を「未確認」に下げ、家で「気になる」に上げる運用が合う。1日20〜30件溜まる想定
- 区域データ約1MBを登録時に読む。電波の弱い場所で「取得中」が長いと言われたら、座標精度を4桁に落とすかタイル分割
- データ更新の周期: 国土数値情報A55は年度ごと。来年度版が出たら `tests/`と同じ手順で `data/zone/` を作り直す(手順は§5)

## §4 残タスク(優先順)

1. ⏳ ソータの実機確認: 建てられる目安 / カメラ撮影→現在地ピン
2. ⏳ 実効面積(駐車場・隣地離れ)を入れるか、ソータの回答待ち
3. ⏳ 招待画面の定型文(位置情報の許可のお願い)
4. ⏳ 周辺施設(Overpass)が実機で入るかは未確認(サンドボックスから叩けない)
5. ⏳ 設定パネル幅(50vw≒195px)が狭くないか、実機の感触待ち
6. ⏳ 地番の自動表示(登記所備付地図データ)。購入導線が決まってから

## §5 そのまま渡せる指示文

### 両リポジトリへの反映(land-hunterが正、tasobowはミラー)
```bash
cd /home/user/land-hunter && git add -A && git commit -m "..." && git push -u origin main
rm -rf /home/user/tasobow/docs && mkdir /home/user/tasobow/docs && cp -r /home/user/land-hunter/. /home/user/tasobow/docs/ && rm -rf /home/user/tasobow/docs/.git && git -C /home/user/tasobow add docs/ && git -C /home/user/tasobow commit -q -m "..." && git -C /home/user/tasobow push -u origin claude/land-search-sharing-app-rxa9k6
until curl -s https://theesota.github.io/land-hunter/js/app.js | grep -q "<新しい関数名>"; do sleep 10; done; echo deployed
```
必ず絶対パスで。cwdはコマンドごとにリセットされる。

### テスト
```bash
cd /home/user/land-hunter && (python3 -m http.server 8776 >/dev/null 2>&1 &)
(npx --yes firebase-tools emulators:start --only firestore --project land-hunter-dev > /tmp/emu.log 2>&1 &)
cd tests && npm i playwright firebase-tools && node zonetest.mjs
```
Chromiumは `/opt/pw-browsers/chromium`、proxy設定は各テスト冒頭にある。

### 区域区分データの作り直し(来年度版が出たら)
国土数値情報の一覧JS `https://nlftp.mlit.go.jp/ksj/js/tokei_data20XX.js` から citycode 10204 の GEOJSON zip URL を拾い、`senbiki`(AreaCode 22=市街化区域→layer1, 23=調整区域→layer2)、`youto`(YoutoName/YoutoCode/BCR/FAR→n/c/bcr/far)、`tokuteiyouto`(DistName→n)を座標5桁に丸めて `data/zone/isesaki_{zoning,youto,tokutei}.geojson` に書く。

### Firebase
- プロジェクト land-hunter-54449、設定は `js/firebase-config.js`(公開情報)。ルールは `firestore.rules`(boardIdが20文字以上ならread/write、listは不可)
- ブラウザ→Firestore/Overpass/gstaticはサンドボックスから届かない。同期はエミュレータ、ルール検証はcurl REST

## §6 反省・学び

- 「先回りの確認」がSafariで裏目に出た。Permissions APIを待ってから位置を取ったら許可ダイアログが出なくなった。ユーザー操作に紐づくAPIは、操作の同期処理の中で即呼ぶ
- URLを綺麗にするためにハッシュを消したのが「共有されない」の根本原因だった。見た目より、識別子はURLに残す
- 「サーバーが空なら守る」保護ルールが、正当な最後の1件の削除を巻き戻した。保護は「サーバーで一度も確認できていないもの」に限定する
- 2018年の広域データで済ませかけたが、ソータの一言で市提出の2024年度データに辿り着いた。自治体ものは「市区町村コード単位の最新年度」を最初に探す
- iOSの案内文は、実機で効いた手順が正。Webの二次情報より、ソータのスクショ1枚の方が強い
- 変更後は必ず両リポジトリにpushして公開URLでgrep確認。ここを省くと「反映されてない」の往復が起きる
